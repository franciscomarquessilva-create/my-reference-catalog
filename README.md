# my-reference-catalog
Reference Catalog is a lightweight, extensible web application that exposes standardized reference artifacts — such as ontologies, taxonomies, models, schemas, and configuration assets — through APIs and structured files (XML, JSON, YAML).
